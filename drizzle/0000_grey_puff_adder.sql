-- Baseline reset: schema already exists in production; this no-op exists only to populate __drizzle_migrations.
-- See drizzle.archive/ for prior history (out-of-sync journal that broke db:migrate).
SELECT 1;
